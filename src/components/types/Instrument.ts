export interface Instrument {
  ticker: string;
  bid: string;
  ask: string;
  ytm: string;
  currentYield: string;
  couponRate: string;
  faceValue: string;
  daysToMaturity: string;
  modifiedDuration: string;
  bondType: string;
  series: string;
}